# Turso / LibSQL Connection Settings
# For local development, leave DATABASE_URL as file:sqlite.db and DATABASE_AUTH_TOKEN empty.
# For production Turso deployment, fetch these from the Turso dashboard.
DATABASE_URL=file:sqlite.db
DATABASE_AUTH_TOKEN=
